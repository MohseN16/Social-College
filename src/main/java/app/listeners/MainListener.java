
package app.listeners;

import org.debux.webmotion.server.WebMotionServerListener;
import org.debux.webmotion.server.call.ServerContext;

public class MainListener implements  WebMotionServerListener {

    @Override
    public void onStart(ServerContext sc) {
        
    }

    @Override
    public void onStop(ServerContext sc) {
        
    }
    
}
